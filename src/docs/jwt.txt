mkdir -p config/jwt

# For Git Bash on Windows
ssh-keygen -t rsa -b 4096 -f config/jwt/private.pem

# For Linux
openssl genrsa -out config/jwt/private.pem -aes256 4096
openssl rsa -pubout -in config/jwt/private.pem -out config/jwt/public.pem